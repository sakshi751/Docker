on:
  push

name: ci

jobs:
  ci:
    runs-on: ubuntu-latest
    
    steps:
    - uses: docker-practice/actions-setup-docker@master
    - run: |
        set -x

        docker version 
        docker run -t postman/newman run "https://services.odata.org/v4/northwind/northwind.svc/Customers"

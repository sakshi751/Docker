on:
  push

name: postman

jobs:
  ci:
    runs-on: ubuntu-latest
    
    steps:
    - uses: docker-practice/actions-setup-docker@master
    - run: |
        set -x

        docker version 
        docker run -t postman/newman run "Weather-API.postman_collection.json"

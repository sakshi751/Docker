name: CI
on:
  push:
    branches: [ main ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: postman/newman:latest
    steps:
      - name: Check for dockerenv file
        run: echo Hello World
